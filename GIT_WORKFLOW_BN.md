# 🌀 গিট ওয়ার্কফ্লো (বাংলা গাইড)

## ✨ মূল ধারণা

-   `main` ব্রাঞ্চ = স্থিতিশীল (stable) কোড
-   নতুন ফিচার/ডিজাইন → আলাদা ব্রাঞ্চ
-   কাজ শেষ হলে Pull Request (PR) দিয়ে `main` এ মার্জ করা হবে

------------------------------------------------------------------------

## 🔧 ধাপে ধাপে প্রক্রিয়া

### ১. নতুন ব্রাঞ্চ তৈরি করা

``` bash
git checkout -b new-feature
```

👉 এখানে `new-feature` এর জায়গায় আপনার কাজ/ফিচারের নাম দিন (যেমন
`new-hero`)

------------------------------------------------------------------------

### ২. কোড লিখুন + কমিট করুন

``` bash
git add .
git commit -m "নতুন হিরো সেকশন যোগ করা হলো"
git push -u origin new-feature
```

------------------------------------------------------------------------

### ৩. GitHub এ Pull Request (PR)

-   আপনার ব্রাঞ্চ → `main` এ PR দিন
-   টিম রিভিউ করলে PR মার্জ করুন

------------------------------------------------------------------------

### ৪. Merge হলে

``` bash
git checkout main
git pull origin main
```

👉 এখন আপনার লোকাল `main` আপডেটেড হয়ে যাবে।

------------------------------------------------------------------------

## 📊 ভিজ্যুয়াল ফ্লোচার্ট

            ┌──────────────┐
            │   main       │   ← আগের লুক (stable version)
            └─────┬────────┘
                  │
                  │ git checkout -b new-hero
                  ▼
            ┌──────────────┐
            │  new-hero    │   ← নতুন লুক (experiment branch)
            └──────────────┘
                  │
                  │ কোড লিখে commit + push
                  ▼
            ┌─────────────────┐
            │ GitHub → PR/Merge│
            └─────────┬───────┘
                      │
                      ▼
            ┌──────────────┐
            │   main       │   ← নতুন লুক merge হয়ে গেছে
            └──────────────┘

------------------------------------------------------------------------

## 📌 টিপস

-   কখনো সরাসরি `main` এ কোড লিখবেন না\
-   প্রতিটি নতুন কাজের জন্য নতুন ব্রাঞ্চ ব্যবহার করুন\
-   PR মার্জের আগে অবশ্যই টেস্ট করুন ✅
